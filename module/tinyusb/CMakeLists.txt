if(RTOS_ENABLED)
    if(IS_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}/tinyusb_freertos")
        add_subdirectory(tinyusb_freertos)
    endif()
else ()
    if(IS_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}/tinyusb_baremetal")
        add_subdirectory(tinyusb_baremetal)
    endif()
endif()
